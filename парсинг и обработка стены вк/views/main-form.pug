// Сегмент выбора условий и кнопка
.main-container
  .container
    // Успех
    #success_message.alert.alert-success.hidden(role='alert')
      strong Ура!
      |  Срамота подчищена.
    // Ошибка
    #error_message(style='width:100%; height:100%; display:none; ')
      h3 Ошибка
      |           Извините, произошла ошибка.
    // Форма
    form#messageForm.form-inline(enctype='multipart/form-data', method='POST')
      .row
        .col-lg-12
          h5.center-block.title-descr
            | Стереть записи, набравшие
      // Ошибка
      #error.col-sm-12(style='color: #ff0000; margin-top: 5px; margin-bottom: 5px;')
      .row
        .form-group.has-feedback
          .col-lg-2.descr менее
          .col-lg-2
            input#likes(type='number', name='likes', required='required', value='', placeholder='12', minlength='1', maxlength='6')
            .descr лайков
          span.glyphicon.form-control-feedback
        .form-group.has-feedback
          .col-lg-2.descr менее
          .col-lg-2
            input#reposts(type='number', name='reposts', required='required', value='', placeholder='10', minlength='1', maxlength='6')
            .descr репостов
          span.glyphicon.form-control-feedback
        .form-group.has-feedback
          .col-lg-2.descr менее
          .col-lg-2
            input#comments(type='number', name='comments', required='required', value='', placeholder='0', minlength='1', maxlength='6')
            .descr комментариев
          span.glyphicon.form-control-feedback
      // Кнопки, отправляющие форму по AJAX
      .row
        .col-lg-12
          button#btnDel.btn.btn-primary.pull-right.btn-lg.btn-success(name='send-message', type='submit', data-loading-text="Обработка...")
            | Очистить стену
